clusterDomain: marketshop.local

architecture: standalone

global:
  storageClass: 'local-path'

auth:
  enabled: false

master:
  nodeSelector:
    node: node1
  persistence:
    enable: false
  disableCommands: []
